import { NmiTokenResponse } from '../types';
/**
 * Creates a token with NMI API for payment processing
 */
export declare function createNmiToken(tokenizationKey: string): Promise<NmiTokenResponse>;
